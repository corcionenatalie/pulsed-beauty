import React from "react";
import ReactDOM from "react-dom/client";
import PulsedBeautyApp from "./App.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <PulsedBeautyApp />
  </React.StrictMode>
);
